import React from "react";
import { Typography } from "antd";

const Exchanges = () => {
  return (
    <div style={{ margin: "20px" }}>
      <Typography.Title level={4}>
        Exchanges are not available yet
      </Typography.Title>
    </div>
  );
};

export default Exchanges;
